# Generate a random API key: openssl rand -hex 32
# Leave empty to run without authentication (not recommended for production)
MCP_API_KEY=

# Your domain pointing to this server's IP
DOMAIN=kari.snowy-burbot.com
